<resources>

    <!--
        Base application theme for API 14+. This theme completely replaces
        AppBaseTheme from BOTH res/values/styles.xml and
        res/values-v11/styles.xml on API 14+ devices.
    -->
    <style name="AppBaseTheme" parent="android:Theme.Material.Light">
        <!-- API 14 theme customizations can go here. -->
        <!--translucent action bar can be enabled here -->
        <item name="android:windowBackground">@color/light_gray</item>
        <!--<item name="android:windowTranslucentStatus">true</item>-->
        <!--<item name="android:windowTranslucentNavigation">true</item>-->

        <item name="android:colorPrimary">@color/primary</item>
        <item name="android:colorPrimaryDark">@color/primary_dark</item>
        <item name="android:textColorPrimary">@color/text_primary</item>
        <item name="android:textColorHint">@color/second_grey</item>
        <item name="android:textColorHighlight">@color/text_blue_grey</item>
        <item name="android:textColor">@color/text_blue_grey</item>
        <item name="android:textColorAlertDialogListItem">@color/primary_dark</item>
        <item name="android:editTextColor">@color/text_blue_grey</item>
        <item name="android:colorMultiSelectHighlight">@color/white</item>
        <item name="android:actionMenuTextColor">@color/white</item>
        <!--<item name="android:navigationBarColor">@color/primary_dark</item>-->
    </style>

</resources>
